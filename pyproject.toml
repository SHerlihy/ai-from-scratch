[project]
name = "ai-from-scratch"
version = "0.1.0"
description = "A GPT-like language model built from tokenizer and data pipeline fundamentals."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic-settings",
    "tiktoken",
    "torch",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
